from threading import Thread
import time
def mostraNome(string):
    while True:
        print(string)
        time.sleep(0.5)

for i in range(10):
    texto = "tread : ",i
    t = Thread(target=mostraNome, args=(texto,))
    t.start()
